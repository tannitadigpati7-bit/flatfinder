// Paste your deployed Google Apps Script Web App URL here to enable a
// shared, live listings database (see README.md > "Setting up the shared backend").
// Leave empty to keep using local seed data + per-browser storage only.
const CONFIG = {
  SHEET_API_URL: "",
};
